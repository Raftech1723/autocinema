<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Gestión de Confitería</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/fondo.jpg'); 
            background-size: cover;
            background-position: center;
            color: white;
            backdrop-filter: blur(5px);
        }

        .container {
            background-color: rgba(0, 0, 0, 0.7);
            padding: 2rem;
            border-radius: 15px;
            margin-top: 2rem;
        }

        .table {
            background-color: white;
            color: black;
        }

        .form-control, .btn {
            border-radius: 10px;
        }

        .navbar {
            background-color: rgba(0, 0, 0, 0.85);
        }

        .navbar-brand {
            color: #f8f9fa;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><img src="/icon.jpg" alt="Logo" style="width: 30px; height: 30px; margin-right: 8px;">AutocineNova</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="btn btn-outline-light me-2" th:href="@{/usuarios}">Usuarios</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/categorias}">Categorías</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/estacionamientos}">Estacionamientos</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/sedes}">Sedes</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/funciones}">Funciones</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/peliculas}">Peliculas</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/salas}">Salas</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/confiterias}">Confiteria</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/metodoPago}">Pagos</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div class="container mt-5">
    <h1 class="text-center mb-4">Gestión de Confiterias</h1>

	<!-- Formulario de Usuario -->
	<div class="card mb-4 shadow">
        <div class="card-body">
    <form th:action="@{/confiterias/guardar}" th:object="${confiteria}" method="post">
        <input type="hidden" th:field="*{id}" />

        <div class="mb-3">
                    <label class="form-label">Nombre</label>
                    <input type="text" th:field="*{nombre}" class="form-control" placeholder="Ingrese el nombre" required>
                </div>
		<div class="mb-3">
        			<label class="form-label">Precio:</label>
        			<input type="number" th:field="*{precio}" class="form-control" placeholder="Ingrese el precio" required />
        		</div>
		<div class="mb-3">
        			<label class="form-label">Descripción:</label>
        			<input type="text" th:field="*{descripcion}" class="form-control" placeholder="Descripción" required />
        		</div>

        <button type="submit" class="btn btn-primary">Guardar</button>
    </form>
  </div>
 </div>
          
    <!-- Tabla de Confiterias -->
    <div class="table-responsive">
        <table class="table table-bordered table-hover align-middle">
            <thead class="table-dark">
            <tr>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Descripción</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="confiteria : ${listaConfiterias}">
                <td th:text="${confiteria.nombre}"></td>
                <td th:text="${confiteria.precio}"></td>
                <td th:text="${confiteria.descripcion}"></td>
                <td>
                    <a th:href="@{/confiterias/editar/{id}(id=${confiteria.id})}" class="btn btn-warning btn-sm">Editar</a> |
                    <a th:href="@{/confiterias/eliminar/{id}(id=${confiteria.id})}" 
                       class="btn btn-danger btn-sm" onclick="return confirm('¿Estás seguro de eliminar?');">Eliminar</a>
                </td>
            </tr>
        </tbody>
    </table>
    </div>
</div>
<!-- Bootstrap JS opcional -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>                                
</body>
</html>