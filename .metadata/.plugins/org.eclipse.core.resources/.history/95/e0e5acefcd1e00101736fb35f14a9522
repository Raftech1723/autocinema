<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Gestión de Películas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/fondo.jpg');
            background-size: cover;
            background-position: center;
            color: white;
            backdrop-filter: blur(5px);
        }
        .container {
            background-color: rgba(0, 0, 0, 0.7);
            padding: 2rem;
            border-radius: 15px;
            margin-top: 2rem;
        }
        .table {
            background-color: white;
            color: black;
        }
        .form-control, .btn {
            border-radius: 10px;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><img src="/icon.jpg" alt="Logo" style="width: 30px; height: 30px; margin-right: 8px;">AutocineNova</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="btn btn-outline-light me-2" th:href="@{/usuarios}">Usuarios</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/categorias}">Categorías</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/estacionamientos}">Estacionamientos</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/sedes}">Sedes</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/funciones}">Funciones</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/peliculas}">Peliculas</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/salas}">Salas</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/confiterias}">Confiteria</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light" th:href="@{/metodoPago}">Pagos</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div class="container mt-5">
    <h1 class="text-center mb-4">Gestión de Películas</h1>

    <!-- Formulario de Película -->
    <div class="card mb-4 shadow">
        <div class="card-body">
            <form th:action="@{/peliculas/guardar}" method="post" th:object="${pelicula}">
                <input type="hidden" th:field="*{id}"/>
                <div class="mb-3">
                    <label class="form-label">Título</label>
                    <input type="text" th:field="*{titulo}" class="form-control" placeholder="Título de la película" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Descripción</label>
                    <textarea th:field="*{descripcion}" class="form-control" placeholder="Descripción" required></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">Duración (minutos)</label>
                    <input type="number" th:field="*{duracion}" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">ID de Categoría</label>
                    <input type="number" th:field="*{categoria_id}" class="form-control" required>
                </div>
                <button type="submit" class="btn btn-primary">Guardar Película</button>
            </form>
        </div>
    </div>

    <!-- Tabla de Películas -->
    <div class="table-responsive">
        <table class="table table-bordered table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Título</th>
                    <th>Descripción</th>
                    <th>Duración</th>
                    <th>ID Categoría</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="p : ${listaPeliculas}">
                    <td th:text="${p.id}">1</td>
                    <td th:text="${p.titulo}">Título</td>
                    <td th:text="${p.descripcion}">Descripción</td>
                    <td th:text="${p.duracion}">120</td>
                    <td th:text="${p.categoria_id}">2</td>
                    <td>
                        <a th:href="@{'/peliculas/editar/' + ${p.id}}" class="btn btn-warning btn-sm">✏️ Editar</a>
                        <a th:href="@{'/peliculas/eliminar/' + ${p.id}}" class="btn btn-danger btn-sm" onclick="return confirm('¿Eliminar esta película?')">🗑️ Eliminar</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>